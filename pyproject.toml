[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sensehatsensorstomqtt"
version = "0.0.4"
description = "sense-hat sensors data to mqtt"
readme = "README.md"
authors = [
  { name = "Olof Hellqvist", email = "olof.hellqvist@gmail.com" }
]
license = { text = "MIT" }
requires-python = ">=3.6"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Build Tools",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.6",
]
keywords = ["sense-hat", "temperature", "mqtt"]
dependencies = [
    "numpy<2.0.0",
    "sense-hat",
    "paho-mqtt",
    "pyyaml",
    "setproctitle",
    "psutil",
]

[project.urls]
Homepage = "https://github.com/hellqvio86/sense-hat-sensors-to-mqtt"
Download = "https://github.com/hellqvio86/sense-hat-sensors-to-mqtt/archive/v_01.tar.gz"

[project.optional-dependencies]
tests = [
    "pyyaml",
    "pytest",
]

[project.scripts]
sensehatsensorstomqtt = "sensehatsensorstomqtt.main:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
